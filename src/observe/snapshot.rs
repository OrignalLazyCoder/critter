#[derive(Debug, Clone, Default)]
pub struct OsSnapshot {
    pub battery_pct: Option<f32>,
    pub battery_health_pct: Option<f32>,
    pub charging: bool,
    pub cpu_temp_c: Option<f32>,
    pub thermal_throttled: bool,
    pub fan_rpm: Option<u32>,
    pub power_assertions_active: bool,
    pub scheduled_wake_count: u32,
    pub display_sleep_minutes: Option<u32>,
    pub sleep_prevented: bool,
    pub cpu_pct: f32,
    pub per_core_cpu_sig: Option<String>,
    pub mem_pct: f32,
    pub gpu_pct: Option<f32>,
    pub neural_engine_pct: Option<f32>,
    pub swap_used_pct: Option<f32>,
    pub load_avg_1m: Option<f32>,
    pub load_avg_5m: Option<f32>,
    pub load_avg_15m: Option<f32>,
    pub system_uptime_secs: Option<u64>,
    pub packet_loss_count: Option<u64>,
    pub kernel_panic_sig: Option<String>,
    pub memory_hungry_process_sig: Option<String>,
    pub wifi_rssi: Option<i32>,
    pub wifi_ssid: Option<String>,
    pub active_interface: Option<String>,
    pub network_up: bool,
    pub net_rx_kbps: u32,
    pub net_tx_kbps: u32,
    pub vpn_active: bool,
    pub key_wpm: f32,
    pub idle_secs: u64,
    pub key_event_age_s: f32,
    pub mouse_move_age_s: f32,
    pub mouse_click_age_s: f32,
    pub scroll_event_age_s: f32,
    pub mouse_click_rate: u32,
    pub shortcut_burst: bool,
    pub clipboard_changed: bool,
    pub clipboard_change_rate: u32,
    pub active_app: String,
    pub active_title: String,
    pub active_app_pid: Option<u32>,
    pub running_apps_sig: Option<String>,
    pub active_window_count: u32,
    pub active_window_geom_sig: Option<String>,
    pub active_space_id: Option<String>,
    pub app_unresponsive: bool,
    pub spotlight_opened: bool,
    pub mission_control_active: bool,
    pub dock_autohide: bool,
    pub dnd_enabled: bool,
    pub notification_center_visible: bool,
    pub display_count: u16,
    pub display_resolution_sig: Option<String>,
    pub screen_brightness_pct: Option<f32>,
    pub true_tone_sig: Option<String>,
    pub dark_mode: bool,
    pub night_shift_enabled: bool,
    pub screen_locked: bool,
    pub screensaver_active: bool,
    pub media_playing: bool,
    pub now_playing_app: Option<String>,
    pub now_playing_track: Option<String>,
    pub output_volume_pct: Option<f32>,
    pub input_volume_pct: Option<f32>,
    pub alert_volume_pct: Option<f32>,
    pub output_muted: bool,
    pub mic_active: bool,
    pub audio_output_device: Option<String>,
    pub audio_output_transport: Option<String>,
    pub headphones_connected: bool,
    pub airplay_active: bool,
    pub system_alert_sound_active: bool,
    pub disk_used_pct: f32,
    pub mounted_volumes_sig: Option<String>,
    pub external_volume_count: u16,
    pub disk_io_mbps: Option<f32>,
    pub time_machine_running: bool,
    pub watched_dir_sig: Option<String>,
    pub large_write_sig: Option<String>,
    pub trash_item_count: u32,
    pub downloads_sig: Option<String>,
    pub ssd_smart_status: Option<String>,
    pub ssd_healthy: bool,
    pub bluetooth_power_on: bool,
    pub bluetooth_connected_count: u16,
    pub bluetooth_connected_sig: Option<String>,
    pub bluetooth_battery_sig: Option<String>,
    pub airpods_connected: bool,
    pub airpods_in_ear: bool,
    pub usb_device_count: u16,
    pub usb_devices_sig: Option<String>,
    pub thunderbolt_connected_count: u16,
    pub thunderbolt_connected_sig: Option<String>,
    pub external_display_usbc: bool,
    pub console_user: Option<String>,
    pub clamshell_closed: bool,
    pub timezone_sig: Option<String>,
    pub boot_time_epoch: Option<u64>,
    pub boot_session_uuid: Option<String>,
    pub shutdown_sig: Option<String>,
    pub restart_sig: Option<String>,
    pub calendar_active_sig: Option<String>,
    pub calendar_active_count: u16,
    pub calendar_max_duration_mins: u16,
    pub calendar_back_to_back: bool,
    pub accessibility_enabled: bool,
    pub reduce_motion_enabled: bool,
    pub increase_contrast_enabled: bool,
    pub voiceover_enabled: bool,
    pub focused_ui_sig: Option<String>,
    pub focused_ui_value_sig: Option<String>,
    pub selected_text_sig: Option<String>,
    pub scroll_position_sig: Option<String>,
    pub location_sig: Option<String>,
    pub location_lat: Option<f64>,
    pub location_lon: Option<f64>,
    pub weather_condition_sig: Option<String>,
    pub sunrise_sig: Option<String>,
    pub sunset_sig: Option<String>,
    pub daylight: Option<bool>,
    pub process_count: u32,
    pub top_process: Option<String>,
    pub top_process_cpu_pct: f32,
    pub ts: u64,
}
